/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ["cdn-icons-png.flaticon.com", "encrypted-tbn0.gstatic.com", "ai-previews.123rf.com"],
    }
};

export default nextConfig;

