/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ["cdn-icons-png.flaticon.com", "encrypted-tbn0.gstatic.com", "images.nightcafe.studio"],
    }
};

export default nextConfig;

